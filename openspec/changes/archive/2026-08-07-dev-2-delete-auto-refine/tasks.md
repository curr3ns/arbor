## 1. Capture the pre-state (must run before anything is deleted)

- [x] 1.1 Record the current `~/.claude/skills/` symlink inventory to a scratch file so the post-install comparison has a baseline: `for l in "$HOME"/.claude/skills/*; do [ -L "$l" ] && printf '%s -> %s\n' "$(basename "$l")" "$(readlink "$l")"; done | sort | tee "${TMPDIR:-/tmp}/arbor-skills-before.txt"`. Verify: the file exists, is non-empty, and contains a line `arbor-auto-refine -> <this repo>/.claude/skills/arbor-auto-refine/` — confirming refine is currently a live symlink into this repo and that the prune therefore has something real to remove.
- [x] 1.2 Confirm the working tree is clean apart from `openspec/` before starting, so the later scope check can attribute every listed path to this change. Verify: `git status --porcelain` lists nothing outside `openspec/changes/dev-2-delete-auto-refine/`.

## 2. Delete the arbor-auto-refine skill

- [x] 2.1 Remove the skill with `git rm -r .claude/skills/arbor-auto-refine` from the repo root — `git rm`, not a filesystem `rm`, so the deletion lands in the index. Verify: the command reports `rm '.claude/skills/arbor-auto-refine/SKILL.md'`, and `ls .claude/skills/ | grep -x 'arbor-auto-refine'` produces no output.
- [x] 2.2 Confirm the deletion is staged rather than merely on disk. Verify: `git status --porcelain -- .claude/skills/arbor-auto-refine` prints exactly `D  .claude/skills/arbor-auto-refine/SKILL.md` with the `D` in the **first** column (index), not the second.
- [x] 2.3 Confirm nothing was created to replace it. Verify: `ls .claude/skills/` lists 15 directories, one fewer than before, and the diff against the pre-change listing is exactly the removal of `arbor-auto-refine` — no new directory, no rename.

## 3. Confirm install.sh empirically (must run after task 2)

- [x] 3.1 Run `./install.sh` from the repo root. It is idempotent and pre-approved here. Verify: the command exits 0 and its output includes a line matching `✗ .*arbor-auto-refine.*(removed)` — the prune loop reporting the removal.
- [x] 3.2 Assert refine's installed link is gone by every test, including the dangling-symlink case that `-e` alone would miss: `[ ! -e "$HOME/.claude/skills/arbor-auto-refine" ] && [ ! -L "$HOME/.claude/skills/arbor-auto-refine" ] && echo PRUNED`. Verify: prints `PRUNED`; equivalently `ls -la ~/.claude/skills/ | grep -c arbor-auto-refine` returns 0.
- [x] 3.3 Assert every surviving arbor skill link is untouched and still resolves into this repo. From the repo root run: `for n in arbor-auto-developer arbor-auto-roadmap arbor-auto-work arbor-code-commit arbor-code-gencommit arbor-opsx-auto arbor-project-scaffold arbor-workspace-add arbor-workspace-create arbor-workspace-import arbor-workspace-init; do t=$(readlink "$HOME/.claude/skills/$n" 2>/dev/null); case "$t" in "$PWD/.claude/skills/$n"*) echo "ok $n";; *) echo "FAIL $n -> ${t:-MISSING}";; esac; done`. Verify: eleven `ok` lines and zero `FAIL` lines.
- [x] 3.4 Diff the post-run inventory against the task 1.1 baseline and account for every difference. Verify: the only removal is `arbor-auto-refine`; any *additions* are links for skills this repo ships that were simply not installed before (on this machine, the four `openspec-*` skills) — those are install.sh doing its job and are not a regression. No surviving link changed target.
- [x] 3.5 Record the outcome of 3.1–3.4 as the empirical confirmation that `install.sh` needs no change, and leave the file untouched. Verify: `git status --porcelain -- install.sh` prints nothing.
- [x] 3.6 **Only if 3.1–3.4 failed**: fix `install.sh` so the prune removes links for skills the repo no longer ships while preserving the rest. Most likely causes are the trailing slash in the stored link target (the install loop globs `*/`, so `readlink` yields `.../arbor-auto-refine/`) breaking the `case "$(readlink "$dst")" in "$skills_src"/*)` match, or the `[ -e "$dst" ]` dangling test. Fix narrowly — e.g. link `"${skill_dir%/}"`, or test `[ -L "$dst" ] && [ ! -e "$dst" ]` explicitly — with no unrelated refactor. Verify: re-running `./install.sh` now satisfies 3.2 and 3.3, and `git diff install.sh` touches only the linking/pruning logic. If 3.1–3.4 passed, mark this task done with an explicit "not needed — prune confirmed working" note and change nothing.
  - **Not needed — prune confirmed working.** 3.1–3.4 all passed on the first `./install.sh` run; `install.sh` is unchanged.

## 4. Scrub arbor-auto-refine from arbor-auto-roadmap

Five references, at roughly lines 3, 12, 32, 39, and 100 of `.claude/skills/arbor-auto-roadmap/SKILL.md`. Each is a considered rewrite: the result must read as though refine never existed. Nothing else in the file changes.

- [x] 4.1 Rewrite the frontmatter `description` (line 3): drop the trailing "neither arbor-auto-refine nor arbor-auto-developer ever invoke it automatically" roster while keeping the claim it carried — the skill is invoked by a user when there's planning to do, never on a timer and never automatically by another skill. Leave the rest of the description (files under `docs/roadmaps/`, the `docs/roadmaps/<slug>.md#R<n>` item-reference format passed to `arbor-auto-work`, the implemented/gated/merged checkbox meaning) as is. Verify: `grep -n 'arbor-auto-refine' .claude/skills/arbor-auto-roadmap/SKILL.md | head` shows no hit on the description line, and the description still asserts human-invoked-only.
- [x] 4.2 Rewrite the opening framing (lines 12–19), currently "Companion to `arbor-auto-refine` (agent 1) and `arbor-auto-developer` (agent 2) in the continuous dev loop… The other two skills poll what it produces". Replace it with the post-refine world: this skill is the human-invoked planning front end; `arbor-auto-developer` reads `docs/roadmaps/*.md` directly as its work queue and `arbor-auto-work` builds an item and marks it done. Preserve every claim the old paragraph made — outside the loop's cadence, never invoked by the loop, only ever run directly by a human, polls nothing, produces one roadmap and stops, never flips a box in a roadmap it wrote. Do **not** edit `arbor-auto-developer` itself; describing it as reading roadmaps is the intended end state (parent roadmap item R4). Verify: the paragraph contains no "agent 1"/"agent 2" numbering, no "the other two skills", names both surviving consumers, and still contains the polls-nothing and never-flips-a-box claims.
- [x] 4.3 Rewrite the non-goals interrogation step's rationale (lines 31–33), currently "keeps `arbor-auto-refine` from later expanding scope back into something the user deliberately excluded". Give a rationale that needs no agent to exist: the roadmap is the only place an exclusion is written down, and writing it down is what stops scope from being quietly re-expanded on a later planning pass. Keep the step itself ("What this roadmap explicitly does not cover") intact. Verify: the step names no skill, and still explains why non-goals are collected.
- [x] 4.4 Rewrite the item-sizing note (lines 39–41), currently "the same sizing `arbor-auto-refine` already uses when it files issues". Justify "sized like a single OpenSpec change" by the real consumer: one roadmap item becomes one `arbor-auto-work` cycle — one branch, one gate, one merge. Keep the "why plus acceptance criteria" requirement and the "an item too big to phrase that way should become two items" rule verbatim in force. Verify: the step ties sizing to `arbor-auto-work`, and both the acceptance-criteria requirement and the split-in-two rule are still present.
- [x] 4.5 Rewrite the final guardrail (line 100), currently "Neither `arbor-auto-refine` nor `arbor-auto-developer` ever invoke this skill automatically; it is only ever run by a human." The claim must survive in spirit; only the roster is wrong. Phrase it without a roster — no other skill ever invokes this skill automatically; it is only ever run by a human — so it cannot go stale again. Keep the preceding clause ("only ever writes a *new* roadmap or extends one it's re-invoked on — never flips a checkbox and never archives or closes out a roadmap itself") unchanged. Verify: the guardrail asserts no-other-skill-auto-invokes plus human-only, and names no skill that is absent from `.claude/skills/`.
- [x] 4.6 Bump `metadata.version` in the frontmatter from `1.3` to `1.4`. Verify: `grep -n 'version:' .claude/skills/arbor-auto-roadmap/SKILL.md` shows `1.4`, and the frontmatter still parses as valid YAML (delimiters intact, `name`, `description`, `license`, `metadata.author`, `metadata.version` all present).

## 5. Final verification

- [x] 5.1 Confirm the scrub is total. Verify: `grep -n 'arbor-auto-refine' .claude/skills/arbor-auto-roadmap/SKILL.md` produces no output and exits non-zero.
- [x] 5.2 Read `.claude/skills/arbor-auto-roadmap/SKILL.md` end to end and confirm no rewrite left a scar — no stub sentence, dangling "and", orphaned "(agent 2)" label, or claim missing its subject; every rewritten sentence is complete and self-supporting. Verify: the read-through finds each of the five edited passages grammatical in isolation.
- [x] 5.3 Confirm nothing R1 established was lost. Verify: `git diff -- .claude/skills/arbor-auto-roadmap/SKILL.md` shows changes confined to the five references and the version line — the interrogation steps, the roadmap template, the item-reference section, the archive note, and every other guardrail are untouched.
- [x] 5.4 Confirm `arbor-auto-developer`'s refine references are still present and unmodified — this is the expected, correct outcome, resolved later by parent roadmap item R4. Verify: `grep -c 'arbor-auto-refine' .claude/skills/arbor-auto-developer/SKILL.md` returns 9, and `git status --porcelain -- .claude/skills/arbor-auto-developer` prints nothing.
- [x] 5.5 Confirm the scope discipline held. Verify: `git status --porcelain` lists, outside `openspec/`, only the staged deletion under `.claude/skills/arbor-auto-refine/` and a modification to `.claude/skills/arbor-auto-roadmap/SKILL.md` — plus `install.sh` if and only if task 3.6 was required. Specifically, no path under `.claude/skills/arbor-auto-work/`, `arbor-auto-developer/`, `arbor-opsx-auto/`, or `arbor-project-scaffold/` appears.
- [x] 5.6 Confirm `docs/roadmaps/roadmap-native-workcycles.md` is untouched — R2's checkbox is bookkeeping handled outside this change. Verify: it does not appear in `git status --porcelain`.
- [x] 5.7 Confirm the change stays branch-local. Verify: `git log --oneline -1` still points at the commit that was `HEAD` before implementation, and `git branch --show-current` is unchanged — no commit, branch, push, or merge was performed.
- [x] 5.8 Read the two delta specs in `openspec/changes/dev-2-delete-auto-refine/specs/` and confirm every requirement's scenarios now hold against the working tree. Verify: each scenario in `specs/skill-distribution/spec.md` and `specs/roadmap-planning/spec.md` maps to a check performed above and passed.
