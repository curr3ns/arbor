All commands below are run from the repo root. `SKILL` abbreviates
`.claude/skills/arbor-auto-developer/SKILL.md`, which is the only product file
this change may modify. Every `grep` is written to be run verbatim; where a
pattern contains a backtick it is wrapped in single quotes so the shell does not
interpret it.

## 1. Frontmatter

- [x] 1.1 Add a clause to the YAML frontmatter `description` in `SKILL` naming the new foreground `--goal` mode, while leaving the existing scheduled/one-cycle-per-run framing intact. Verify: `grep -n -- '--goal' .claude/skills/arbor-auto-developer/SKILL.md | head -1` reports a line inside the frontmatter block (above the closing `---`), and the description still contains the words that state the skill runs on a schedule and that each run is a single cycle, not a loop.
- [x] 1.2 Confirm the description claims nothing the body does not specify. Verify: read the `description` clause by clause against the body — every behavior it names (roadmap queue, one item, one subagent, merge to `main`, scheduled single cycle, `--goal` foreground mode) is specified in the body, and the body specifies nothing the description contradicts.
- [x] 1.3 Bump `metadata.version` from `"2.0"` to `"2.1"` (design D1: additive mode, not a rewrite). Verify: `grep -n 'version:' .claude/skills/arbor-auto-developer/SKILL.md` shows `version: "2.1"` and no longer shows `"2.0"`.
- [x] 1.4 Confirm the frontmatter still parses. Verify: the block opens and closes with `---`, and `name`, `description`, `license`, and `metadata` are all present; `grep -cE '^(name|description|license|metadata):' .claude/skills/arbor-auto-developer/SKILL.md` returns `4`.

## 2. Mode framing near the top

- [x] 2.1 Add a short mode framing to the opening of `SKILL` (in or immediately after the existing framing paragraph) that names both modes: the default scheduled mode, unattended and cron-paced, and the `--goal` foreground mode, human-invoked and running to a stated finish line. Verify: both modes are named in the first screen of the file, above `## The cycle`, and the contrast is drawn on who invokes the mode and what ends the run — not merely on the presence of a flag.
- [x] 2.2 State the default in the same place the flag is named: without `--goal`, one cycle per invocation. Verify: the sentence naming `--goal` in the framing also states the no-flag default, so a reader forming a plan sees both together.
- [x] 2.3 Confirm `--goal` is not specified as a bullet in a list of invocation options. Verify: every occurrence of `--goal` outside the frontmatter and the mode framing is inside the dedicated goal-mode section; `grep -n -- '--goal' .claude/skills/arbor-auto-developer/SKILL.md` produces no hit in `## The cycle`, `## Subagent dispatch`, or `## Notifications`.

## 3. The goal-mode section and the `/goal` condition

- [x] 3.1 Add a dedicated section `## Foreground goal mode (--goal)` after `## The cycle`. Verify: `grep -n '^## Foreground goal mode' .claude/skills/arbor-auto-developer/SKILL.md` matches exactly once.
- [x] 3.2 Write the condition the mode sets, verbatim in the required form. Verify: `grep -n 'Every item in `docs/roadmaps/<slug>.md` is checked off and the file has been migrated to `docs/roadmaps/archive/`' .claude/skills/arbor-auto-developer/SKILL.md` matches, and the surrounding text says `<slug>` is replaced by the target roadmap's actual slug rather than written literally.
- [x] 3.3 State that `/goal` installs a **session-scoped Stop hook**, that the hook is what keeps the session going between items, and that it auto-clears when the condition holds. Verify: `grep -n 'session-scoped Stop hook' .claude/skills/arbor-auto-developer/SKILL.md` matches, and all three claims (installs, keeps going, auto-clears) are present.
- [x] 3.4 State that the condition names exactly one roadmap and that the archival clause is load-bearing — a completed roadmap has been `git mv`d out of `docs/roadmaps/` by `arbor-auto-work`, so a condition phrased only against the original path would be evaluated against a file that no longer exists. Verify: the reason for the archival clause is written down, not left implicit.
- [x] 3.5 State that the goal is set once at the start of the run and is never re-issued with a different condition mid-session. Verify: the once-only rule is present and phrased as a prohibition on re-issuing.
- [x] 3.6 Confirm the condition is not open-ended. Verify: `grep -niE 'until (the )?roadmaps? (are|is) done|keep working until' .claude/skills/arbor-auto-developer/SKILL.md` returns nothing that is offered as a `/goal` condition string.

## 4. Target roadmap selection

- [x] 4.1 Document that `--goal` takes an optional roadmap argument — a slug or a `docs/roadmaps/<slug>.md` path — and that when given, that roadmap is the target. Verify: both argument forms are named.
- [x] 4.2 State that a named roadmap absent from the non-archived `docs/roadmaps/*.md` stops the run with a report, and that a different roadmap is never selected in its place. Verify: both halves are present, and the "never silently retarget" prohibition is explicit.
- [x] 4.3 State that with no argument the target is the first roadmap holding an eligible item under the skill's **existing** filename-order walk. Verify: the text reuses the existing walk by reference rather than restating or redefining a selection rule; no new ordering key is introduced.
- [x] 4.4 State that the target is resolved once and **pinned** for the run, and give the reason: the condition names one `<slug>`, and re-deriving the walk against a moved `main` could shift work onto a roadmap the condition does not name, which can then never clear. Verify: both the rule and its reason are written down.
- [x] 4.5 State that on resuming after a Stop-hook re-prompt the skill recovers its target from `/goal active` rather than re-running the walk. Verify: `grep -n -- '/goal active' .claude/skills/arbor-auto-developer/SKILL.md` matches, and it is used as the authoritative record of the run's target.
- [x] 4.6 Handle the two degenerate starts: no roadmap holds an eligible item at all, and the named roadmap exists but holds no eligible item. In both, **no goal is set**, the skill reports, and it exits on the same quiet path a scheduled idle tick takes. Verify: both cases are addressed by name, and both explicitly suppress setting a goal.

## 5. The loop works items one at a time

- [x] 5.1 State that the goal run works items **one at a time** — the same single dispatch per cycle the default mode performs, repeated. Verify: the phrasing ties the loop to the existing cycle rather than describing a new algorithm, and no sentence permits selecting or queueing more than one item.
- [x] 5.2 Restate the one-subagent guardrail inside the goal section, scoped to the whole run: exactly one subagent in flight at any moment across the entire goal run, retries included, never two, never a batch. Verify: the restatement appears inside `## Foreground goal mode (--goal)` and says "never two" or equivalent, rather than relying on the Guardrails section alone.
- [x] 5.3 State that each cycle completes fully — dispatch, verification, notification or annotation — before the next begins, and that each cycle re-reads the queue from `main` so the previous cycle's merge or annotation is visible. Verify: both the sequencing and the re-read are stated.
- [x] 5.4 State that under `/goal` the repetition belongs to the **hook**: the skill runs one cycle per turn and then stops, and the Stop hook brings the session back; the skill does not additionally loop internally while a hook is driving the session. Verify: the mechanism is written down and the "set the goal and also loop" double-drive is forbidden by name.
- [x] 5.5 State that dispatch stays autonomous in goal mode — "foreground" means the human is watching the session, not that approvals are prompted — so subagents still run `arbor-auto-work` in its default mode with no `--interaction` and no `--pr`. Verify: the foreground/interactive distinction is written explicitly, and `grep -n -- '--interaction' .claude/skills/arbor-auto-developer/SKILL.md` shows the goal-section occurrence stating the flag is *not* passed.

## 6. Termination and the no-livelock argument

- [x] 6.1 State the single termination condition shared by both paths: no eligible item remains in the target roadmap — either the roadmap is finished and archived, or every unchecked item that remains carries a `<!-- blocked: ... -->` annotation. Verify: one condition is stated for both paths, not two.
- [x] 6.2 State that an all-blocked roadmap is a **terminating state, not a retry state**. Verify: `grep -niE 'terminating state' .claude/skills/arbor-auto-developer/SKILL.md` matches in the goal section, and no sentence directs re-attempting blocked items or running a triage pass over them to keep the loop alive.
- [x] 6.3 Write the termination argument: every iteration either merges the item (one fewer unchecked item) or exhausts its two attempts and annotates it blocked (one fewer *eligible* item); both strictly decrease over a finite set, so the loop reaches "no eligible item" in a bounded number of iterations. Verify: both outcomes and the strict-decrease claim are present, and the conclusion "therefore the loop terminates" is stated rather than left to inference.
- [x] 6.4 Add the session-local exhausted-item record: the goal run remembers items it has exhausted during the run and treats them as ineligible for the remainder of the run, so termination does not depend on the blocked-annotation push having landed. Verify: the record is described, and the reason (R4's race procedure legitimately *drops* the annotation in two cases) is given.
- [x] 6.5 Confirm the goal loop does not invent a re-attempt policy for blocked items. Verify: R4's existing "blocked items wait for a human" rule is untouched, and the goal section adds nothing that contradicts it.

## 7. Clearing the goal when the condition can never hold

- [x] 7.1 Instruct the skill to issue `/goal clear` itself when the loop terminates without the condition holding — the all-blocked case, and any case where the target roadmap is neither finished nor archived. Verify: `grep -n -- '/goal clear' .claude/skills/arbor-auto-developer/SKILL.md` matches, and the instruction is attached to that terminal state.
- [x] 7.2 Give the reason: the condition is false and will stay false, so an active Stop hook would keep re-prompting a session that has correctly concluded there is nothing left to do. Verify: the reason is written down; a reader can tell why the clear is necessary rather than merely being told to do it.
- [x] 7.3 Require the terminating report to name which items are blocked and why. Verify: the report is specified as naming the blocked items and their reasons, so a human can act on it without re-reading the roadmap.
- [x] 7.4 State that when the condition *does* hold — the roadmap completed and archived — the hook auto-clears and no manual `/goal clear` is required. Verify: the asymmetry between the two terminating states is explicit, so the skill neither leaks a live hook nor clears one redundantly.

## 8. `/goal` availability and the fallback

- [x] 8.1 State that `/goal` requires a **trusted workspace** and **unrestricted hooks**. Verify: `grep -n 'trusted workspace' .claude/skills/arbor-auto-developer/SKILL.md` matches, and both preconditions appear.
- [x] 8.2 Name both restricted-hooks settings individually. Verify: `grep -n 'disableAllHooks' .claude/skills/arbor-auto-developer/SKILL.md` and `grep -n 'allowManagedHooksOnly' .claude/skills/arbor-auto-developer/SKILL.md` each match, and each is presented as a cause of `/goal` being unavailable.
- [x] 8.3 Note that Stop hooks are REPL-only, so a non-REPL invocation cannot have one — a further legitimate reason the fallback exists. Verify: `grep -n 'REPL' .claude/skills/arbor-auto-developer/SKILL.md` matches, and the note is framed as an additional cause rather than as a replacement for the two named settings.
- [x] 8.4 Write the fallback: when `/goal` is unavailable, the skill **reports the reason** and **falls back to running cycles back-to-back in-session** until no eligible item remains in the target roadmap. Verify: both the report and the fallback are present, the reason is required to name which of the three causes applied, and the run is not aborted.
- [x] 8.5 Describe the fallback as the same single-item cycle run back-to-back in-session, not a second algorithm, and state that it terminates on the same condition as the `/goal` path. Verify: the fallback introduces no different budget, selection rule, guardrail, or stopping point.
- [x] 8.6 Keep the unavailability report as prose in the session rather than a `PushNotification`. Verify: no new notification trigger is introduced for this report; see task 11.2.

## 9. Completion handoff

- [x] 9.1 State that a goal run finishes one roadmap: when the target completes while other non-archived roadmaps still hold eligible items, the run ends with the condition met and does not roll onto another roadmap. Verify: the prohibition on rolling on is explicit, and its reason (the condition names one `<slug>`; re-issuing a new condition is forbidden by task 3.5) is given or referenced.
- [x] 9.2 Require the run's closing report to name the roadmaps that still hold eligible work and the invocation that would continue with the next one. Verify: both the list and the next invocation are specified.

## 10. Scoping the existing one-cycle statements and the attempt budget

- [x] 10.1 Scope the framing paragraph's "this skill never loops internally" to the default path, naming `--goal` as the exception. Verify: the sentence now names its scope, remains true word for word of any invocation without `--goal`, and is not softened into "usually" or "generally".
- [x] 10.2 Scope the guardrail "**One run = one cycle.** Do not loop internally; exit as soon as the item is handled …" the same way: it governs the default, scheduled invocation. Verify: the guardrail names the checkable condition under which repetition is permitted (`--goal` was passed) rather than being weakened into a preference, and the scheduled reading is unchanged.
- [x] 10.3 State the operative reading of the attempt budget under a loop: at most two attempts — one original plus one retry — **per item**, and an item that exhausts the budget is annotated blocked and thereafter skipped by the walk, so no item receives a third attempt within a goal run either. Verify: the per-item scope is explicit, and the guardrail's force is unchanged.
- [x] 10.4 State that "never move on to a different item in its place" is a **within-cycle** rule: a failed cycle never chains into another item, and the goal run reaches the next item by starting a new cycle whose walk re-reads `main`. Verify: the distinction between chaining inside a cycle and starting a new cycle is written down, not implied.
- [x] 10.5 Re-read tasks 10.1–10.4's edits against a scheduled invocation with no `--goal`. Verify: every one of those sentences is still true word for word on the scheduled path, and the scheduled run still handles exactly one item and exits.
- [x] 10.6 Scope step 6's own statement of the same rule — "never move on to a different item in its place in this run — one run is one cycle, and any other eligible item waits for the next scheduled tick" — the same way 10.2 scoped the guardrail. Without this, the rule is stated at two different scopes in two places: within-cycle in the guardrails, per-run in step 6, and step 6's closing clause reads false during a goal run, where there is no scheduled tick to wait for. Verify: the default-path sentence is preserved word for word and the addition is a scope qualifier naming `--goal`; it restates that the no-third-attempt and no-chaining halves are unchanged under `--goal` and that only the "waits for the next scheduled tick" clause is replaced, by the goal run's next cycle.

## 11. Non-regression of R4's settled behavior

- [x] 11.1 Confirm the settled contracts survive unchanged in force: the roadmap-queue walk and its archive exclusion, the phases-are-strictly-sequential rule, one item per cycle, the dispatch contract and its `{ outcome, work_id, branch, note }` return, the retry-then-annotate path with its push race and abandonment rule, the roadmap-completion observation, the merge-to-`main` target, and the never-author-roadmap-content guardrails. Verify: the retention sweep hits every string — `grep -nF 'You MUST create a todo per step' SKILL`, `grep -nF 'docs/roadmaps/*.md' SKILL`, `grep -nF 'docs/roadmaps/archive/' SKILL`, `grep -nF 'roadmap:docs/roadmaps/<slug>.md#R<n>' SKILL`, `grep -nF '<!-- blocked:' SKILL`, `grep -nF '{ outcome, work_id, branch, note }' SKILL`, `grep -nF 'PushNotification' SKILL`, `grep -nF '**R<n>**' SKILL` — each returning at least one line.
- [x] 11.2 Confirm the notification set is still exactly three triggers and that neither "goal set" nor "goal reached" became a fourth. Verify: the `## Notifications` section enumerates merge landed, item blocked after retry, and roadmap complete, and no fourth trigger appears anywhere; `grep -niE 'notif' .claude/skills/arbor-auto-developer/SKILL.md` yields no line introducing a goal-related notification event.
- [x] 11.3 Re-run R4's removal sweep, minus `--goal` (which this change legitimately introduces), and require zero hits: `grep -nE 'arbor-auto-refine|agent:backlog|priority:|gh auth|gh issue|gh pr|pull request|integration branch|review thread|CHANGES_REQUESTED|Closes #' .claude/skills/arbor-auto-developer/SKILL.md`. Verify: no output.
- [x] 11.4 Confirm every occurrence of `--pr` is still a statement that the flag is **not** passed. Verify: `grep -n -- '--pr' .claude/skills/arbor-auto-developer/SKILL.md` — inspect each hit; each must forbid passing the flag (expect the dispatch section's original plus the goal section's restatement from task 5.5), and none may direct passing it.
- [x] 11.5 Confirm no roadmap-authorship licence leaked in. Verify: the goal mode adds no instruction to flip a checkbox, edit item text, add or rename a phase, invoke `arbor-auto-roadmap`, or move a roadmap file; `grep -nE 'git mv|mkdir' .claude/skills/arbor-auto-developer/SKILL.md` returns nothing directing this skill to perform the move.

## 12. Scope discipline

- [x] 12.1 Confirm the edit is additive. Verify: `git diff -U0 .claude/skills/arbor-auto-developer/SKILL.md` — every removed (`-`) line is accounted for by exactly one of: the frontmatter `description`, the `metadata.version` bump, or a task 10.1/10.2 scope qualifier. No settled rule from the walk, the dispatch section, the annotation path, the notifications, or the guardrails is rewritten or softened.
- [x] 12.2 Confirm nothing outside the one product file and `openspec/` changed. Verify: `git status --porcelain` lists no path other than `.claude/skills/arbor-auto-developer/SKILL.md` and paths under `openspec/`, and specifically no path under `.claude/skills/arbor-auto-work/`, `.claude/skills/arbor-auto-roadmap/`, `.claude/skills/arbor-opsx-auto/`, or `.claude/skills/arbor-project-scaffold/`, and no new skill directory.
- [x] 12.3 Confirm no `docs/roadmaps/archive/` directory, placeholder, or `.gitkeep` was created. Verify: `ls docs/roadmaps/archive` fails, and the path does not appear in `git status --porcelain`.
- [x] 12.4 Confirm `docs/roadmaps/roadmap-native-workcycles.md` is untouched — R5's own checkbox is flipped outside this change, by `arbor-auto-work`'s commit step. Verify: it does not appear in `git status --porcelain`.

## 13. Final verification

- [x] 13.1 Run the goal-mode presence sweep and require a hit for each: `grep -nF -- '--goal' SKILL`, `grep -niF 'foreground' SKILL`, `grep -nF 'session-scoped Stop hook' SKILL`, `grep -nF '/goal clear' SKILL`, `grep -nF '/goal active' SKILL`, `grep -nF 'disableAllHooks' SKILL`, `grep -nF 'allowManagedHooksOnly' SKILL`, `grep -nF 'trusted workspace' SKILL`, `grep -nF 'REPL' SKILL`, and `grep -n 'is checked off and the file has been migrated to' SKILL`. Verify: every one returns at least one line.
- [x] 13.2 Re-read the finished `SKILL.md` against acceptance criterion 1: `--goal` sets `/goal` with a condition naming the target roadmap in exactly the required form, then works items one at a time, relying on the session-scoped Stop hook to keep the session going and auto-clear when the condition holds. Verify: each clause of the criterion is satisfied by a specific sentence in the file, identified by name.
- [x] 13.3 Re-read against acceptance criterion 2: the documented behavior notes that `/goal` requires a trusted workspace and unrestricted hooks, and that when it is unavailable (`disableAllHooks` or `allowManagedHooksOnly`) the skill reports the reason and falls back to running cycles back-to-back in-session until no eligible item remains. Verify: both named settings appear as written, and the report-then-fall-back sequence is present in that order.
- [x] 13.4 Re-read against acceptance criterion 3: the default remains one cycle per invocation, so the scheduled path is unaffected. Verify: an invocation without `--goal` is specified to work exactly one item and exit, and nothing in the scheduled path's prose has changed meaning.
- [x] 13.5 Re-read against acceptance criterion 4: the frontmatter `description` mentions the new mode and `metadata.version` is bumped from `"2.0"`. Verify: both hold, per tasks 1.1 and 1.3.
- [x] 13.6 Read the finished file end to end against every requirement in `specs/roadmap-queue/spec.md`, checking each scenario literally rather than approximately. Verify: every scenario holds when read as written, with particular attention to the two asymmetric terminating states (archived → hook auto-clears; all-blocked → the skill issues `/goal clear`), the pinned target, and the one-subagent guardrail inside the loop.
- [x] 13.7 Read the finished file for internal consistency: the frontmatter description, the mode framing, the cycle steps, the goal-mode section, the dispatch section, the notifications, and the guardrails must all describe the same skill. Verify: no rule is stated in one place and contradicted in another, and no section presupposes text that is not in the file.
- [x] 13.8 Run `openspec validate dev-5-developer-goal-mode --strict`. Verify: it reports the change as valid. Note that `openspec validate --all` fails on pre-existing specs missing a Purpose section; that is out of scope for this change and must not be "fixed" here.
